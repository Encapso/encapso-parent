<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.encapso</groupId>
    <artifactId>encapso-parent</artifactId>
    <version>1.0.0-beta.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Encapso Parent</name>
    <description>Centralized Maven Parent POM for the Encapso Architectural Framework</description>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        
        <encapso.version>1.0-SNAPSHOT</encapso.version>
        <javapoet.version>1.13.0</javapoet.version>
        <junit.version>5.10.0</junit.version>
        <compile-testing.version>0.21.0</compile-testing.version>
        <truth.version>1.1.5</truth.version>
        <jakarta.inject.version>2.0.1</jakarta.inject.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Core Project Modules -->
            <dependency>
                <groupId>io.github.encapso</groupId>
                <artifactId>encapso</artifactId>
                <version>${encapso.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.encapso</groupId>
                <artifactId>encapso-engine</artifactId>
                <version>${encapso.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.encapso</groupId>
                <artifactId>encapso-processor</artifactId>
                <version>${encapso.version}</version>
            </dependency>

            <!-- External Dependencies -->
            <dependency>
                <groupId>com.squareup</groupId>
                <artifactId>javapoet</artifactId>
                <version>${javapoet.version}</version>
            </dependency>
            <dependency>
                <groupId>jakarta.inject</groupId>
                <artifactId>jakarta.inject-api</artifactId>
                <version>${jakarta.inject.version}</version>
            </dependency>

            <!-- Test Dependencies -->
            <dependency>
                <groupId>com.google.testing.compile</groupId>
                <artifactId>compile-testing</artifactId>
                <version>${compile-testing.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-api</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-engine</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>com.google.truth</groupId>
                <artifactId>truth</artifactId>
                <version>${truth.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.1.2</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>3.1.1</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>3.1.1</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
